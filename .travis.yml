sudo: false
language: node_js
node_js:
  - 10
cache: npm
branches:
  only:
    - master
script:
  - hexo generate
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GitHub
  keep-history: true
  on:
    branch: master
  local-dir: public
